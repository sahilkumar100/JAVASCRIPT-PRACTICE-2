// 1. push()
// The push() method adds one or more elements to the end of an array and returns the new length of the array.

// let arr = [1, 2, 3];
// arr.push(4); // Adds 4 to the end of the array
// console.log(arr); // [1, 2, 3, 4]



// 2. pop()
// The pop() method removes the last element from an array and returns that element.

// let arr = [1, 2, 3, 4];
// let removed = arr.pop(); // Removes 4 from the array
// console.log(arr); // [1, 2, 3]
// console.log(removed); // 4



// 3. shift()
// The shift() method removes the first element from an array and returns that element.


// let arr = [1, 2, 3, 4];
// let removed = arr.shift(); // Removes 1 from the array
// console.log(arr); // [2, 3, 4]
// console.log(removed); // 1

 

// 4. unshift()
// The unshift() method adds one or more elements to the beginning of an array and returns the new length of the array.


// let arr = [2, 3, 4];
// arr.unshift(1); // Adds 1 to the beginning of the array
// console.log(arr); // [1, 2, 3, 4]


// 1. For Loop (Array)
// A for loop is a traditional way to iterate over the elements of an array. It gives you access to the index of each element.

// let arr = [10, 20, 30, 40, 50];

// // Using for loop to iterate through the array
// for (let i = 0; i < arr.length; i++) {
//     console.log(arr[i]); // Accessing elements by index
// }





// 2. For...of Loop (Array)
// The for...of loop is a more modern and concise way to iterate over the values of an array directly, without needing the index.


// let arr = [10, 20, 30, 40, 50];

// // Using for...of loop to iterate through the array
// for (let value of arr) {
//     console.log(value); // Directly accessing values
// }
















































